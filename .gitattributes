# 换行一律钉成 LF。仓库内容与平台无关。
# 理由：本项目做逐字节对拍（verify_bin.py 等），检出时的换行差异会让"同一次构建"
# 在不同机器上产出不同字节。不写死的话，Windows 上 core.autocrlf=true 会把 LF 换成 CRLF。
* text=auto eol=lf

# 万一以后要入库的批处理，必须 CRLF（Windows 的 cmd 对 LF 的 .bat 支持有坑）
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制，别做任何转换
*.png  binary
*.ico  binary
*.icns binary
*.nforge binary
*.onnx  binary